server:
   port: 9002
   
spring:
  application:
    name: JOBSEEKER-SERVICE
    
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url: 
       default-Zone : http://localhost:8761/eureka/
  instance:
    hostname: localhost