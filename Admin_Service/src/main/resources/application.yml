server:
   port: 9003
   
spring:
  application:
    name: ADMIN-SERVICE
    
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url: 
       default-Zone : http://localhost:8761/eureka/
  instance:
    hostname: localhost